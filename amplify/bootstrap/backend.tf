# Backend configuration will be provided during terraform init
# Run: terraform init -backend-config="bucket=<bucket-name>" -backend-config="key=bootstrap/terraform.tfstate" -backend-config="region=us-east-2"
#terraform {
#  backend "s3" {}
#}